class Article < ApplicationRecord
  # validates :title, presence: true
  # # validate the length of the body and show a custom message
  # validates :body, presence: true, length: { minimum: 10, message: 'must have at least 10 characters' }
end
